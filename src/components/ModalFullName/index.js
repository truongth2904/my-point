import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ModalFullName = () => {
  return (
    <View>
      <Text>ModalFullName</Text>
    </View>
  );
};

export default ModalFullName;

const styles = StyleSheet.create({});
